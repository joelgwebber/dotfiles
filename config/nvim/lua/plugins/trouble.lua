return {
  'folke/trouble.nvim',
  dependencies = { 'nvim-tree/nvim-web-devicons' },
  opts = {
    use_diagnostic_signs = true,
    -- auto_preview = false,
    -- auto_refresh = false,
    focus = true,
    pinned = true,
    -- auto_open = true,
    -- auto_close = true,
  },
}
