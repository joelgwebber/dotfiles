---
id: dots-0968
title: Reconcile the Linux fish config with the ported one
type: task
priority: 2
created: '2026-09-13T20:33:51Z'
updated: '2026-09-13T20:33:51Z'
parent: dots-3ab4
needs: human
---

The Mac fish config was built fresh from the zsh port (user chose 'build fresh, you reconcile'). The Linux box has its own fish config that has never been in this repo, so the two will collide the moment Linux pulls and runs chezmoi apply.

On Linux, ~/.config/fish is currently UNMANAGED, so chezmoi apply will overwrite config.fish and add conf.d/ + functions/ without warning. Run 'chezmoi diff' there BEFORE applying, and save anything worth keeping.

Things most likely to differ: prompt (this one replicates the old zsh PS1 exactly), key bindings, any fisher plugins, and Linux-specific PATH entries. Anything Linux-only belongs in a runtime 'if test (uname) = Linux' branch or a new conf.d file rather than a template, to match how 00-path.fish already handles linuxbrew and pnpm.

---
▸ 2026-09-13T20:33:51Z [Joel Webber]
On the Linux box: run 'chezmoi diff' before applying, and tell me what your existing fish config has that this port doesn't. I'll merge rather than overwrite.
